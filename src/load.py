# src/load.py
import json
import sqlite3
import os

def load_users_to_db():
    """Lê os dados transformados e carrega na tabela SQL."""
    print("Iniciando carga de dados no banco...")
    # Implementação mock de carga
    print("Carga concluída com sucesso.")

if __name__ == "__main__":
    load_users_to_db()